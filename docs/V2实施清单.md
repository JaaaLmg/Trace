# TRACE V2 实施清单

## 0. 读法

这份文档是接手 TRACE V2 的快读状态源，只放当前事实、核心口径、未覆盖边界和分工接口。

- V2.1 已完成记录统一浓缩到 `docs/V2实施归档.md`。
- V2.2 最终整合验收看 `docs/V2.2_最终整合验收报告.md`。
- A 线执行平台细节看 `docs/V2.2-A线执行平台说明.md`；V2.3 执行规模化对接看 `docs/V2.3_A_对接文档.md`。
- B 线项目理解/数据集/生成约束对接：V2.2 看 `docs/V2.2_B_对接文档.md`，V2.3 看 `docs/V2.3_B_对接文档.md`，V2.4 看 `docs/V2.4_B_对接文档.md`。
- 评测算法与项目理解的原因、取舍和分工原则看 `docs/评测与项目理解.md`。
- 技术契约最终以代码、schema、测试和真实评测证据为准。

## 1. 当前结论

TRACE V2.3 A/B 主体已通过最终整合验收，下一阶段进入 V2.4 最终实训/展示验收版。

当前验收状态：

- V2.3 A 线执行平台/评测尺子：通过。
- V2.3 B 线项目理解/数据集/生成约束：通过。
- V2.3 A/B 合同收束：通过。
- 后端全量自动化：通过。
- 前端生产构建：通过。
- 补丁格式检查：通过。

最终整合验收结果：

```text
V2.2 backend/tests: 362 passed, 2 skipped, 3 warnings
V2.3 backend/tests: 386 passed, 2 skipped, 3 warnings
frontend npm run build: passed
git diff --check: passed
```


## 2. V2.2 已交付能力

### A 线：评测尺子 / 执行平台

- Runtime profile 已升级为执行合同，支持 executor、image、working dir、install/test command、timeout、network/resource/artifact/cleanup policy，并冻结到 clean run 与 replay evidence。
- Executor 抽象支持 `local_subprocess` 与 `docker`。
- `local_subprocess` 明确是本地进程执行，不是安全沙箱，不能宣称 enforce 网络或资源隔离。
- Docker executor MVP 已落地：检查 Docker CLI/daemon/image，构造受控 workspace mount，收集 pytest JSON/JUnit/stdout/stderr/container metadata。
- setup/install stage 是 first-class execution step；setup failure 进入 `setup_failed` EvaluationEvent，不算 0 捕获。
- Replay 不调用 LLM，只执行冻结测试集；replay evidence 包含 runtime snapshot、executor metadata、workspace manifest、artifact hash、cache key/status/source。
- Incremental replay cache 已落地，cache hit/stale/miss 可审计，失败 job 不会写成捕获结果。
- Replay scheduler 支持单机并发 variant replay；clean replay 与 flaky gate 仍先于同 clean unit 的 variant replay。
- Metrics / EvaluationEvent / 前端 replay panel 已展示 executor、runtime、setup、cache、cleanup、parallel replay evidence。
- Runtime profile API 与前端管理页支持 list/create/update/archive；实验创建页可选择 runtime profile 并展示 executor/policy summary。
- Runtime 页面已支持中英文切换；API 枚举值保留原文。

### B 线：项目理解 / 数据集 / 生成约束

- Source context 会把目标函数引用到的同文件模块级常量作为 dependency evidence。
- retrieval trace 记录 resolved / missing / truncated / failed，不静默吞证据。
- `source_context.py` 已机械拆出 failure context、trace helper、dependency slicing、support helper、route helper、target query helper。
- Contract Guard 会消费静态常量证据，可拒绝无源码依据的 route path 参数值。
- Contract Guard 已约束 route method/path、response model 字段、request field evidence、fixture evidence、弱 oracle、目标漂移和目标重定义。
- route request field guard 覆盖 `json=`、`params=`、`data=`，并限制在 HTTP verb 调用上，避免误伤普通 helper 的同名关键字。
- EvaluationEvent prompt-safe policy 已抽到 `evaluation_event_policy.py`。
- `source_context_failures.py` 低层过滤 audit-only / non-consumable EvaluationEvent；`provider_failure`、`artifact_hash_mismatch` 不进 prompt。
- Dataset UI 已接入 task / seeded bug / ordinary variant / mutation discovery / confirm-selected 最小链路。
- Dataset UI 展示结构 readiness 和 mutation discovery exclusion reason/target/message；readiness 不是 replay 结果，也不解释成捕获率。
- mutation confirmation 不信任客户端回传 audit candidate，服务端会重算 discovery 并逐字段比对 selected candidate。
- mutation confirmation 只允许 `bug_type=auto_mutation`；普通 variant API 拒绝 `ground_truth.source=auto_mutation` 绕过 probe confirmation。
- auto mutation v0 是最小 confirmation 链路，不是完整 mutation engine。

## 3. V2.3 当前状态

本节是 A/B 共同视角，细节事实源分别看 `docs/V2.3_A_对接文档.md` 和 `docs/V2.3_B_对接文档.md`。

1. Dataset Authoring 闭环已补齐：task / seeded bug / variant 可创建、编辑、检查；readiness、search/filter、confirmation 状态和失败原因可见。
2. Mutation Engine v1 已收束为最小可验收集：comparison_boundary / comparison_negation / arithmetic_operator / boolean_negation / constant_replacement / return_value；仍不宣传为完整 mutation testing 平台。
3. Execution Scale & Runtime UX 已形成对接合同：runtime profile replay_policy、experiment runtime_profile_bindings、多 project dataset runtime binding manifest、replay retry/cache、artifact inventory、workspace cleanup 和 Runtime preflight 已接入 API / 前端。
4. Metrics / evidence 口径保持 V2.2 硬边界：replay 不调用 LLM，setup/executor/provider/artifact/flaky/invalid failure 不压成 0 捕获，sampled mutation score 只统计 confirmed + probe passed auto mutation variant。
5. Contract Guard retry 已增强真实 API 场景的可修复反馈，但 Guard 仍是系统边界，不是 warning 或 prompt 装饰。
6. V2.3-M4 联合验收已通过：Dataset -> Experiment -> Metrics / Evidence / Report 链路进入 V2.4 最终展示收束。

仍是后续候选、不是当前 V2.3 验收阻塞项：

- Prompt / Tool schema 管理页。
- source context 剩余 retrieval provider glue 机械拆分。
- 更复杂 retry/rerun 策略和事件过滤决策持久化。
- 批量实验级 scheduler、metrics projection cache、跨实验/跨数据集 replay cache 治理。
- cleanup policy 自动执行、Docker executor 更多宿主系统兼容性扩展。
- 分布式 replay / 跨机器执行；当前仍是单机模型，不开启 V3 远程 Runner。
- `exception_type` / `statement_deletion` 等高风险 mutation operator；必须先补 probe 或 AST patch 合同。

## 4. V2.3 规划与分工

V2.3 目标是从“V2.2 已验收”推进到“可持续扩展”。主线不是重新证明链路能跑，而是把 Dataset、mutation 和执行规模化三块从最小可用推进到稳定产品能力。

### 4.1 总目标

- Dataset 从最小 authoring 推进到可管理、可审计、可复用。
- auto mutation 从 v0 confirmation 链路推进到受控 mutation engine v1。
- 执行平台从单机完整链路推进到更可靠、更可观测、更适合批量实验。
- 保持 V2.2 硬口径不退化：invalid/flaky/provider/setup/artifact/runtime failure 仍不能算 0 捕获。

### 4.2 当前收束点

- Dataset authoring：结构编辑、readiness、筛选、confirmation 状态展示已形成最小闭环。
- Mutation v1：已形成明确 operator set，并保留 discovery evidence、deterministic selection、server-side rediscovery 和 probe confirmation。
- Runtime / replay：runtime profile replay_policy、runtime binding manifest、replay retry/cache、artifact inventory 和 cleanup evidence 已形成对接合同。
- Metrics / events：runtime_execution、EvaluationEvent、sampled mutation denominator 和 prompt-safe evidence 边界已明确。

### 4.3 分工边界

| 分工 | 当前 V2.3 责任 | 对接输出 |
| --- | --- | --- |
| A 线 | executor、runtime profile、experiment runner、replay、cache/cleanup、artifact inventory、metrics evidence | runtime profile / binding manifest、runtime snapshot、replay/cache/cleanup evidence、metrics / EvaluationEvent |
| B 线 | source context、retrieval trace、Contract Guard、Dataset UI、mutation confirmation、prompt-safe 回灌 | dataset authoring/readiness、mutation discovery/confirmation evidence、guard violation / retry prompt contract、prompt-safe source context |
| A/B 共同 | schema/API/migration、跨页面展示、验收报告和演示口径 | Dataset -> Experiment -> Metrics / Evidence / Report 的可解释闭环 |

### 4.4 联合验收关注点

- 多 project dataset 必须显式选择 runtime profile，不再靠前端扫描 project/snapshot 推断。
- replay 继续只执行冻结测试集，`llm_calls=0`。
- provider/setup/executor/artifact/flaky/invalid failure 继续作为有效性状态展示，不解释成 0 捕获。
- confirmed + probe passed auto mutation variant 才能进入 sampled mutation score 分母。
- prompt 只吃 prompt-safe evidence；audit-only event 默认只进入 report/audit。
- `local_subprocess` 继续只作为本地执行方式，不宣传为沙箱；Docker executor 仍按 MVP 能力描述。

### 4.5 里程碑

| 里程碑 | 目标 | 主责 | 当前状态 / 验收 |
| --- | --- | --- | --- |
| V2.3-M1 Dataset Authoring 闭环 | UI 完整创建和检查 task / seeded bug / variant；mutation confirmation 状态和失败原因可见 | B 线 | 已收束；Dataset API 测试、前端 build、readiness/confirmation smoke |
| V2.3-M2 Mutation Engine v1 | 最小 operator set；每个 operator 有 discovery evidence、selection reason、probe check | B 线 | 已收束；mutation discovery/confirm 合同测试、sampled mutation score 分母测试 |
| V2.3-M3 Execution Scale & Runtime UX | batch/replay/cache/runtime profile 体验增强；多 project dataset 不再含糊 | A 线 | 已形成对接合同；runtime profile、binding manifest、replay cache/cleanup、metrics 目标回归 |
| V2.3-M4 整合验收 | A/B 合同收束，形成 V2.3 验收结论 | A/B 共同 | 已通过；后端全量、前端 build、diff check 通过，清单已更新 |

### 4.6 暂不做

- 不做 V3 远程 Runner 协议、Runner 注册、heartbeat、long-poll 领任务。
- 不做分布式调度、MinIO/S3 artifact storage 或完整 OpenTelemetry。
- 不把 auto mutation v1 宣传成完整 mutation testing 平台。
- 不把 local_subprocess 包装成安全沙箱。
- 不改变 captured bug、invalid_test_set、false positive、sampled mutation score 的解释口径。
- 不把 audit-only event 默认塞回 prompt。

## 5. V2.4 最终验收版规划

V2.4 的目标不是开启 V3 平台化，而是把 V2.3 的可持续能力收束成最终实训/展示验收版。判断标准不是“功能更多”，而是一次完整 benchmark 能被非开发者看懂、能复跑、能解释为什么可信。

### 5.1 总目标

- 固化一套可展示的 Dataset benchmark：至少覆盖 demo seeded bug 数据集，并优先补 1-2 个小型真实项目或扩展 demo project。
- 完成 Direct / Plan / ReAct 三策略的最终对比跑法，真实 LLM 与 MockLLM 分开解释。
- Docker runtime 至少有稳定 smoke 和一条可演示链路；local subprocess 继续只作为本地执行方式，不宣传为沙箱。
- 前端支撑最终演示：实验对比、创建实验后的启动入口、replay 证据、Dataset 可见入口、Dataset 状态、mutation confirmation、runtime evidence、report/export 路径都能讲清楚。
- 文档收束：最终验收报告、演示 runbook、已知边界、V3 候选不混在一起。

### 5.2 必须交付

- Dataset 最终包：task、seeded bug、variant、probe 证据、readiness 状态和筛选视图稳定。
- Mutation v1 收束：有一组明确 operator，但只统计 confirmed + probe passed variant。
- Prompt / Tool schema 最小管理能力：至少能记录版本、查看内容/hash，并和 strategy snapshot 对齐。`provider/model` 选择以后端 LLM 配置/合同为事实源，前端只展示可用选项并提交 override。
- Final benchmark report：包含 capture、false positive、invalid、flaky、provider/setup/artifact/runtime failure 的解释。
- 前端最终演示路径：从可见 Dataset 入口进入 Dataset，创建 Experiment 后能在详情页直接启动，再到 Metrics / Evidence / Report，不需要手工查 DB、手输隐藏路由或返回列表找启动按钮才能讲清楚。

### 5.3 V2.4 分工

V2.4 是最终实训/展示验收版，不开启 V3 平台化。分工按工程边界延续，但共同目标是把一条可展示 benchmark 讲清楚、跑得通、解释得可信。

| 分工 | V2.4 重点 | 验收输出 |
| --- | --- | --- |
| A 线：执行与评测证据 | 固化 runtime profile、Docker smoke、replay/cache/cleanup、artifact lifecycle、runtime failure 展示和 metrics/report 稳定性 | 可复跑实验、runtime/replay evidence、Docker/local executor 边界说明、metrics/report 输出 |
| B 线：Dataset 与生成约束 | 固化展示 Dataset benchmark、Dataset 可见入口、Dataset readiness/confirmation、mutation v1 evidence、Contract Guard/prompt-safe 口径、真实 LLM/MockLLM 解释边界 | Dataset benchmark 包、可直接进入的 Dataset UI 演示入口、mutation confirmation evidence、Guard/invalid_test_set 解释、prompt-safe source context 说明 |
| A/B 共同 | Direct / Plan / ReAct 同 Dataset 对比、Experiment 创建后启动入口、最终演示 runbook、V2.4 验收报告、已知边界和 V3 候选分离 | 从 Dataset -> Experiment -> Start -> Metrics / Evidence / Report 的演示路径，最终验收报告和演示步骤 |

### 5.4 V2.4 里程碑

| 里程碑 | 目标 | 主责 | 验收 |
| --- | --- | --- | --- |
| V2.4-M1 Benchmark 固化 | 固定一套可展示 Dataset benchmark，覆盖 task / seeded bug / variant / probe / readiness，并提供前端可见入口 | B 线主责，A 线配合 runtime 可跑性 | Dataset 可从 UI 直接进入，readiness 通过，mutation confirmed evidence 完整 |
| V2.4-M2 三策略实验 | Direct / Plan / ReAct 在同一 Dataset 上完成可解释对比，创建后的 draft 实验可在详情页直接启动 | A/B 共同 | capture、false positive、invalid、flaky、runtime failure 解释一致；启动 API、状态刷新和详情页入口一致 |
| V2.4-M3 Runtime 演示链路 | Docker runtime 至少稳定 smoke，一条 local/Docker 可演示链路 | A 线 | replay 不调用 LLM，failure 不压成 0 捕获，artifact/cache/cleanup evidence 可见 |
| V2.4-M4 最终文档与演示 | 最终验收报告、演示 runbook、已知边界、V3 候选分开 | A/B 共同 | 后端全量、前端 build、diff check 通过；报告和 runbook 完成 |

### 5.5 验收门槛

| 验收项 | 标准 |
| --- | --- |
| 后端回归 | `conda run -n trace pytest backend/tests -q` 通过 |
| 前端构建 | `npm run build` 通过 |
| 格式检查 | `git diff --check` 通过 |
| 三策略 bench | Direct / Plan / ReAct 在同一 Dataset 上完成可解释对比 |
| Docker smoke | 至少一条实验或 replay 链路可用 Docker executor 跑通 |
| 指标口径 | invalid/flaky/provider/setup/artifact/runtime failure 不算 0 捕获 |
| 文档 | V2.4 最终验收报告、演示步骤、已知边界同步完成 |

V2.4 B 线可独立收束项已进入可交付状态，细节以 `docs/V2.4_B_对接文档.md` 为准。

V2.4 A 线 runtime / metrics / report 收束已经完成，细节以 `docs/V2.4_A_对接文档.md` 为准；最终演示路径和验收口径已补充到 `docs/V2.4_演示runbook.md` 与 `docs/V2.4_最终验收报告.md`。当前 A/B 共同口径是：从 Dataset -> Experiment -> Start -> Metrics / Evidence / Report 的 UI 链路可以走通；Start 只允许 draft experiment 原地启动；cleanup/replay-cache cleanup 必须先 dry-run 展示候选并二次确认；Comparison API 模式不再回退 demo metrics；LLM override 创建合同由后端校验 selectable option。

### 5.6 前端演示 UX 收束状态

这些问题不是 V3 平台化需求，而是 V2.4 最终实训/展示验收前必须补齐的“可讲、可点、可复跑”断点。当前 A/B 演示链路已经按最小验收口径收束；下表保留状态和剩余边界，方便后续接手。

| 界面 | V2.4 状态 | 主责 | 剩余边界 |
| --- | --- | --- | --- |
| 顶部导航 / Dataset | 已完成：顶部 Dataset 入口进入 Dataset 列表；API 模式列出已有 Dataset，Demo 模式显示固定 demo benchmark | B 线主责，A 线补齐列表入口 | Dataset 列表仍是最小选择页，不做复杂筛选/权限 |
| Dataset authoring | 已完成最小闭环：demo preset、snapshot 下拉、ordinary variant preset、readiness/confirmation 失败原因可见 | B 线主责 | 新建完整真实 Dataset 仍需人工补 task/bug/variant/probe；建议最终演示优先使用 seed demo |
| Mutation confirmation | 已完成最小闭环：结构化 probe 表单、只读 Probe JSON、selected candidate 填充样例 | B 线主责 | auto mutation 仍不是完整 mutation testing 平台 |
| Experiment 创建 | 已完成：LLM option 从后端读取，缺 dataset/runtime/strategy/LLM 的阻塞原因可见；URL dataset 不存在时会 warning | A/B 共同 | 多 project Dataset 只做显式 runtime binding，不做批量模板 |
| Experiment 详情 | 已完成：draft 可直接 Start，queued/running/completed/failed/cancelled 不误导原地重跑；progress 自动刷新 | A/B 共同 | cancelled/failed 原地 rerun 不做，后续走 clone/new experiment 合同 |
| Runtime evidence / cleanup | 已完成：preview 候选表、二次确认、默认 keep_failed、中文按钮文案 | A 线主责 | cleanup 只清理 workspace/cache 文件，不重写历史 metrics |
| Runtime profile | 已完成：executor 能力、isolation/network/resource limit、preflight checks、replay policy 和 audit-only limitation 可见 | A 线主责 | Docker smoke 依赖宿主 Docker CLI/daemon/image，可不可用但必须解释 |
| Comparison | 已完成：空状态串起 Dataset/Experiment/Start/Completed；API 失败不回退 demo metrics | A/B 共同 | 仍是单 experiment 内三策略对比，不做跨 Dataset 聚合 |
| Report / export | 已完成最小闭环：metrics JSON 下载、first run report、Dataset 回指、Runtime execution JSON | A/B 共同 | experiment-level Markdown report 生成服务留作增强项 |

### 5.7 V2.4 不做

- 不做 V3 远程 Runner 协议、Runner 注册、heartbeat、long-poll 领任务。
- 不做分布式调度、MinIO/S3 artifact storage 或完整 OpenTelemetry。
- 不把 auto mutation 包装成完整 mutation testing 平台。
- 不引入新的大策略线；策略对比以能稳定复现和解释为主。

## 6. A/B 分工边界

V2.2/V2.3 分工按工程边界记录，不按 Direct / Plan / ReAct 策略分工。策略是共同评测对象，不是代码所有权边界。

| 分工 | 责任边界 | 稳定输出 | 红线 |
| --- | --- | --- | --- |
| A 线：评测尺子 / 执行平台 | executor、runtime profile、experiment runner、replay、metrics、DB/API evidence | `EvaluationEvent`、experiment metrics、replay artifact、dataset/variant 状态机、runtime evidence | provider failure、flaky、artifact mismatch、invalid test set、setup/executor failure 不能压成 0 捕获；执行边界不能宣传成未实现的安全沙箱 |
| B 线：项目理解 / 数据集 / 生成约束 | source context、retrieval trace、Contract Guard、Dataset UI、mutation confirmation、prompt-safe 回灌 | generated test metadata、target mapping、guard violation、source context trace、dataset/variant 编辑证据 | 证据不足时必须拒绝、降级或标记 incomplete；不能靠 prompt 自律替代 guard；auto mutation v0 不能宣传为完整 mutation engine |

协作接口：

- A 输出稳定的 `EvaluationEvent`、experiment metrics、replay artifact、runtime evidence 和 dataset/variant 状态机。
- B 消费 prompt-safe evidence，输出可 replay 的 generated test metadata、target mapping、guard violation 和 source context trace。
- A 不依赖 B 的 prompt 内部文本；B 不依赖 A 的内部 executor/metrics payload，只依赖稳定事件和 schema。
- 共同维护 schema / migration / 前端展示口径；任何跨线字段先写合同测试，再接 UI 或 runner。

## 7. 关键口径

- clean run 失败、provider failure、executor failure、setup failure、artifact hash mismatch、flaky clean replay 是系统/评测有效性问题，不是 0 捕获。
- replay 不调用 LLM；`llm_calls` 必须为 0。
- mutation score 只统计可评价测试集。
- false positive 是 clean 代码上测试失败，必须和 variant 捕获分开。
- real LLM 与 MockLLM 结果分开解释。
- prompt 输入只吃 prompt-safe evidence；审计事件不能默认塞回 LLM。
- source context 缺目标源码、缺模型字段、缺 fixture 证据时，必须拒绝、降级或标记 incomplete。
- Contract Guard 是系统边界，不是提示词装饰。
- `backend/llm.config.json` 和 `.env*` 永远不入库。

## 8. 最终验证

| 命令 | 结果 |
| --- | --- |
| `conda run -n trace pytest backend/tests -q` | 386 passed，2 skipped，3 warnings |
| `npm run build`（frontend） | passed |
| `git diff --check` | passed |

非阻塞 warning：

- FastAPI TestClient 的 Starlette/httpx deprecation warning。
- `TestPlan` / `TestRun` ORM model 类名触发 pytest collection warning。
- Git 读取用户全局 ignore 文件权限不足，不影响仓库内 diff/status 判断。

## 9. 文件职责

- `docs/V2实施清单.md`：当前事实源、未覆盖边界和分工接口。
- `docs/V2.2_最终整合验收报告.md`：V2.2 最终整合验收结果和交付结论。
- `docs/V2.2-A线执行平台说明.md`：V2.2 A 线执行平台事实源。
- `docs/V2.3_A_对接文档.md`：V2.3 A 线 runtime profile、replay/cache/cleanup、metrics evidence 对接事实源。
- `docs/V2.2_B_对接文档.md`：V2.2 B 线项目理解/数据集/生成约束对接事实源。
- `docs/V2.3_B_对接文档.md`：V2.3 B 线 Dataset authoring、mutation confirmation 和 Contract Guard retry 对接事实源。
- `docs/V2.4_B_对接文档.md`：V2.4 B 线 Dataset / LLM option / 演示链路对接事实源。
- `docs/V2.4_A_对接文档.md`：V2.4 A 线 runtime / metrics / report / cleanup / LLM option 合同收束事实源。
- `docs/V2.4_演示runbook.md`：V2.4 可复现演示步骤和本机联调记录。
- `docs/V2.4_最终验收报告.md`：V2.4 最终验收结论、指标口径和 V3 候选。
- `docs/V2实施归档.md`：V2.1 收工归档和证据索引。
- `docs/评测与项目理解.md`：为什么这么做，以及后续分工原则。
