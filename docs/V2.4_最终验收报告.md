# TRACE V2.4 最终验收报告

更新日期：2026-06-21

## 1. 结论

V2.4 A/B 合并后已完成最终实训/展示验收所需的演示链路收束。当前版本可以从 Dataset 进入创建实验，在 Experiment detail 启动 draft experiment，并在 completed 后查看 Metrics、Evidence、Runtime、Cleanup 和 Report / Export 入口。

2026-06-21 已在 API 模式生成一条完整 MockLLM 三策略验收实验：`v24-final-api-20260621233841`，状态 `completed`。该实验验证了 Dataset -> Experiment -> Start -> Metrics / Evidence -> Report / Export 的最终展示链路。

本轮没有开启 V3 能力，不包含远程 Runner、分布式调度、对象存储 artifact 或完整 OpenTelemetry。

## 2. 本轮完成

- LLM option 从前端收口升级为后端创建合同校验：`llm_override` 必须匹配 selectable option。
- LLM config error 响应脱敏，不回显 secret、base_url、原始坏配置或本地路径。
- Experiment Start 状态机与后端对齐：只有 draft 可原地启动。
- Runtime profile preflight 展示明细 check、replay policy 和 audit-only limitation。
- Experiment detail 支持 workspace cleanup / replay cache cleanup 预览候选、二次确认和执行结果。
- Runtime evidence、Replay evidence、EvaluationEvent 和 metrics 继续围绕“不把系统失败压成 0 捕获”的口径展示。
- Report / Export 最小闭环完成：metrics JSON 下载、run-level report 入口、Dataset 回指。
- Comparison API 模式不再回退 demo metrics，避免污染真实 benchmark 证据。

## 3. B 线对接

- Dataset readiness 继续是结构检查，不是 replay 结果。
- Mutation confirmation 的前端结构化 probe 只生成既有 Probe JSON，信任根仍是服务端 rediscovery + probe check。
- sampled mutation score 分母只统计 confirmed + probe passed 的 auto mutation variant。
- Contract Guard 仍是系统边界；pipeline reject / invalid test set 不能讲成 prompt warning。
- prompt-safe evidence 与 audit-only evidence 继续按 EvaluationEvent / backfeed policy 区分。

## 4. 指标口径

- replay 不调用 LLM，`llm_calls=0`。
- invalid_test_set 不算 0 捕获。
- provider failure 不算 0 捕获。
- setup/executor/runtime failure 不算 0 捕获。
- artifact hash mismatch 不算 0 捕获。
- flaky clean replay 不算 0 捕获。
- false positive 是 clean baseline 失败，必须和 variant capture 分开。
- zero capture 只用于可评价测试集确实没有捕获 bug 的情况。

## 5. Runtime 边界

- local_subprocess 是本地进程执行，不是安全沙箱。
- Docker executor 是当前可演示的隔离执行路径，但依赖宿主 Docker CLI/daemon/image。
- Docker 不可用时应展示为 executor unavailable 或 preflight failed/warning，不进入捕获率分母。
- cleanup 只清理 workspace/cache 文件，不重写历史 metrics。

## 6. 验证记录

本轮已执行：

```powershell
npm run build
conda run -n trace pytest backend/tests/test_llm_options_api.py backend/tests/test_stage4_experiment_runner.py -q --basetemp=D:\tmpdir\Temp\trace-v24-final-stage4
conda run -n trace pytest backend/tests/test_artifact_cleanup.py backend/tests/test_replay_scheduler.py -q --basetemp=D:\tmpdir\Temp\trace-v24-final-cleanup
conda run -n trace pytest backend/tests -q --basetemp=D:\tmpdir\Temp\trace-v24-final-all-2
git diff --check
```

结果：

- frontend build passed。
- LLM option / experiment runner 目标测试：31 passed，3 warnings。
- cleanup / replay scheduler 目标测试：15 passed。
- 后端全量：391 passed，2 skipped，3 warnings。
- `git diff --check` passed。

验收中发现并修复了一个合并回归：`POST /api/v1/experiments` 的路由曾先捕获 `ValueError`，导致继承自 `EvaluationError` 的 `ExperimentConflictError(status_code=409)` 被错误映射成 400。当前已调整为先捕获 `ExperimentError`，后端全量回归通过。

API 演示链路证据：

| 项 | 结果 |
| --- | --- |
| Experiment | `v24-final-api-20260621233841` |
| Dataset | `dataset-demo-v2` / `TRACE demo seeded bugs` |
| Runtime profile | `e2c47195-f8b2-447f-9aeb-85b620be8553` / local subprocess |
| LLM option | MockLLM `mock/mock-1`；真实 LLM option 仅展示为 configured |
| Readiness | `ready` |
| Experiment status | `completed` |
| Clean runs | 3 |
| Replay runs | 27 |
| Replay LLM calls | `[0]` |
| EvaluationEvent count | 54 |
| Artifact inventory | 3 artifacts |
| Workspace cleanup dry-run | 9 candidates |
| Replay cache | 27 entries |
| Replay cache cleanup dry-run | 0 candidates |
| Runtime preflight | `warning`，4 checks |
| Docker smoke | unavailable：Docker Desktop Linux engine pipe 不存在 |
| Dataset 回指 | `#/datasets/dataset-demo-v2` |
| Experiment detail | `#/experiments/v24-final-api-20260621233841` |
| First run report | `#/runs/993a7566-3b36-4b10-8023-4e82da0c2826` |

三策略指标：

| Strategy | metric_status | capture_rate_mean | false_positive_rate |
| --- | --- | --- | --- |
| `direct` | `ok` | 0.8333 | 1.0 |
| `plan_execute` | `ok` | 1.0 | 0.0 |
| `react_reflection` | `ok` | 1.0 | 0.0 |

备注：

- Direct 的 false positive 是本次 MockLLM 结果证据，必须和 variant capture 分开解释，不能抹成 0。
- Docker 不可用是 runtime 环境状态，不进入捕获率分母。
- 本次 API 验收使用 FastAPI TestClient + Celery eager 执行真实 API route，不依赖手工查 DB。

## 7. 后续交付记录建议

最终展示时建议补充 UI 截图或录屏：

- Dataset detail。
- Experiment detail completed 状态。
- Metrics / Evidence / Runtime execution。
- cleanup dry-run candidates。
- Report / Export 区域。

## 8. V3 候选

以下不属于 V2.4 验收承诺：

- 远程 Runner、register、heartbeat、long-poll。
- 分布式调度与跨机器 replay。
- MinIO/S3 artifact storage。
- 完整 OpenTelemetry。
- experiment-level Markdown 报告生成服务。
- cancelled/failed experiment 的原地 rerun；后续应设计 clone/new experiment 合同。
