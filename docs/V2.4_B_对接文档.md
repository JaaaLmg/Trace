# TRACE V2.4 B 线对接文档

## 1. 分工边界

V2.4 B 线继续负责 Dataset、mutation confirmation、Contract Guard、prompt-safe evidence 和前端演示链路中属于数据集/生成约束的部分。本轮第一刀只收束最终实训/展示验收的可见断点，不开启 V3 平台能力。

本轮没有改变以下硬边界：

- 不做远程 Runner、register、heartbeat、long-poll 或分布式调度。
- replay 仍只执行冻结测试集，不调用 LLM。
- invalid_test_set、provider_failure、artifact_hash_mismatch、setup/executor failure、flaky clean replay 不能算 0 捕获。
- Contract Guard 仍是系统边界，不是 prompt 装饰。
- prompt 只吃 prompt-safe evidence；audit-only 默认只进 report/audit。
- 不修改 backend/llm.config.json、.env.local 或任何密钥文件。

## 2. 本轮新增对接点

### 2.1 LLM 选项只读合同

新增后端只读 API：

- `GET /api/v1/llm-options`

返回内容只包含前端创建实验所需的 provider/model 选择合同：

- MockLLM 选项始终可用，用于确定性演示和 harness 验证。
- 后端运行配置存在真实 LLM provider/model 时，返回一个 configured 选项。
- 真实 LLM 缺少 API key 时标记 `selectable=false`，前端展示但不能选择。
- 响应不返回 `api_key`、`base_url` 明文或任何密钥值，只返回 `base_url_configured` 布尔状态。

该 API 的事实源是后端 LLM runtime config。前端不硬编码真实 provider/model，只消费该 API 返回的可选项并提交 `llm_override`。

### 2.2 Experiment 创建页 provider/model 收口

Experiment 创建页移除裸 provider/model 输入框，改为 LLM option 下拉：

- MockLLM / mock-1 明确显示为 Mock/scripted。
- 后端配置的真实 LLM 明确显示为真实 LLM。
- 不可选项显示 unavailable，不能被提交。
- 创建 experiment 时提交所选 option 的 provider/model override；最终 resolved provider/model 仍以 clean run `strategy_snapshot` 为审计事实。

这解决了 V2.4 演示中“前端随便填 provider/model”的断点，同时不把真实配置文件推给前端。

### 2.3 Dataset 可见入口

顶部导航新增 Dataset 入口：

- demo 数据源直达 `dataset-demo-v2`，方便从 Dataset benchmark 开始演示。
- API 数据源进入现有 Dataset authoring 入口。
- Experiment 列表中的 `dataset_id` 改为可点击入口，能直接跳到对应 Dataset detail。

这只补可见路径，不新增 Dataset schema，不改变 readiness 或 mutation confirmation 口径。

### 2.4 Experiment 详情页启动入口

Experiment detail 页面新增 Start 按钮：

- 仅 API 数据源显示。
- 仅 draft / queued / cancelled 状态可启动。
- 调用既有 `POST /api/v1/experiments/{experiment_id}/runs`。
- 启动后刷新 progress，并保留原有 metrics/evidence 展示路径。

该入口属于 A/B 共同演示链路，但本轮只接前端入口，不改变 experiment runner 状态机。

### 2.5 Dataset authoring 演示样例填充

Dataset detail 的 authoring 面板新增最小样例填充能力：

- Task 表单的 project snapshot 改为优先从当前 dataset 的 `project_snapshot_ids` 下拉选择；没有可选 snapshot 时保留手填。
- Task 表单提供 demo target scope / goal / capability 样例填充。
- Ordinary variant 表单提供 ground truth / patch 样例填充，但仍禁止 `ground_truth.source=auto_mutation` 绕过 confirmation。
- Mutation discovery 选中 candidate 后，会基于 candidate evidence 预填 confirm-selected 表单和 probe JSON；服务端仍会 rediscovery 并执行 probe check。

该切片只减少演示手填，不新增 Dataset schema，不改变 readiness、probe confirmation 或 sampled mutation score 分母口径。

### 2.6 Dataset 到 Experiment 创建链路

Dataset detail 顶部新增创建实验入口：

- 仅 API 数据源、已加载真实 dataset detail 时显示。
- 点击后跳转到 `#/experiments?dataset=<dataset_id>`。
- Experiment 创建页读取该 query，并在 dataset option 存在时预选对应 dataset。
- query 只影响前端默认选择，不改变 `createExperiment` API payload，也不绕过 runtime profile / strategy / LLM option 必填校验。

这补齐 Dataset -> Experiment 的演示链路，A 线仍只需要按既有 Experiment 创建合同处理后续 runner / metrics / report。

### 2.7 Mutation confirmation 结构化 probe 表单

Mutation discovery 的 confirm-selected 面板新增结构化 probe 输入：

- `target_kind`、`probe`、`clean_value`、`buggy_value` 分开填写，`clean_value` / `buggy_value` 仍按 JSON 值解析。
- 选择 selected candidate 时，前端用 candidate evidence 预填结构化字段。
- 面板保留只读 Probe JSON 预览，确认前会从结构化字段重新生成原合同 JSON。
- 服务端仍执行 rediscovery、candidate 匹配和 probe check；前端表单不直接确认 mutation，也不绕过 clean/buggy 期望值校验。

该切片只降低手写 JSON 成本，不把 mutation confirmation 包装成完整 mutation testing 平台。

## 3. A/B 接口约定

- B 线前端可以展示 experiment metrics / EvaluationEvent / prompt-safe 边界，但不解释 A 线 executor 内部 payload。
- LLM option API 只解决创建 experiment 的配置选择，不改变 run 级 `strategy_snapshot` 冻结规则。
- Dataset readiness 继续是结构检查，不是 replay 结果，也不是 captured bug 指标。
- Dataset authoring preset 只写入可编辑表单，不直接创建 task / bug / variant。
- Dataset -> Experiment query 只做 UI 预选，不是后端合同字段。
- Mutation confirmation 结构化字段只生成既有 Probe JSON 合同；sampled mutation score 分母口径不变。
- sampled mutation score 分母仍只统计 confirmed + probe passed 的 auto mutation variant。

## 4. 当前测试计划

本轮应验证：

```powershell
conda run -n trace pytest backend/tests/test_llm_options_api.py -q
cd frontend; npm run build
git diff --check
```

若后端合同影响 experiment 创建路径，再补跑相关 experiment runner / schema 测试。

## 5. 后续 B 线候选

- 与 A 线共同完成 Direct / Plan / ReAct 同 Dataset 对比的最终演示脚本。

## 6. B 线演示口径与 A 线对接清单

本节不是最终 A/B 演示 runbook。最终 runbook 应在 A 线 runtime / metrics / report 入口收束后统一编写。

本节只固定 B 线讲解口径，方便 A 线对接 `Metrics / Evidence / Report` 时不误读 Dataset、mutation confirmation、Contract Guard 和 prompt-safe evidence。

### 6.1 Dataset 口径

- Dataset 入口用于展示 benchmark 分母来源：task、seeded bug、variant、readiness 和 confirmation 状态。
- Dataset readiness 是结构检查，不是 replay 结果，也不是捕获率。
- Dataset authoring preset 只填入可编辑表单，不直接创建 task / bug / variant。
- 普通 seeded bug variant 不进入 sampled mutation score 分母。

### 6.2 Mutation confirmation 口径

- Mutation discovery dry-run 不写库、不跑 replay。
- Confirm-selected 会服务端 rediscovery 并校验 candidate；前端 audit report 不是信任根。
- 结构化 probe 表单只生成既有 Probe JSON 合同。
- sampled mutation score 分母只统计 `ground_truth.source=auto_mutation` 且 `probe.probe_check.status=passed` 的 confirmed variant。
- probe failed / probe missing / ordinary variant 都不能算进 confirmed mutation 分母。

### 6.3 Experiment 创建口径

- Dataset detail 的创建实验入口跳转到 `#/experiments?dataset=<dataset_id>`，query 只做 UI 预选。
- Runtime profile / strategy / LLM option 仍必须显式选择。
- provider/model 只能来自 `GET /api/v1/llm-options`；MockLLM 与真实 LLM 的边界要在创建页讲清楚。
- Experiment detail 的 Start 入口只接现有 runner API，不改变 A 线状态机。

### 6.4 Metrics / Evidence 口径

- replay 不调用 LLM；replay 只执行冻结测试集。
- `invalid_test_set`、`provider_failure`、`artifact_hash_mismatch`、setup/executor failure、flaky clean replay 不能解释成 0 捕获。
- Contract Guard 拒绝是系统边界，不能讲成 prompt warning。
- Contract Guard / pipeline reject 应进入 invalid_test_set 或明确失败路径。
- EvaluationEvent 默认是 audit evidence；只有 prompt-safe 子集才能回灌给生成/反思。
- audit-only evidence 默认只进 report/audit，不进入 prompt。
- A 线 runtime evidence 可以展示 executor、setup、artifact、cache、cleanup 状态；B 线不解释 executor 内部 payload。

### 6.5 Report 对接提醒

- 最终 Report 入口应由 A/B 合并 runbook 固定，不要求用户查 DB 或手找文件。
- Report 中的 Dataset / Experiment / Metrics / Evidence 需要能回指到本节口径。
- 若 Report 尚未暴露某项 evidence，演示时应标成 A/B 待接入口，不能口头补成“已经完整展示”。
