# TRACE V2 实施归档

> 本文件归档自精简前的 `docs/V2实施清单.md`，保留长历史、旧失败流水、完整验收清单和验证表。
> 当前状态源请优先读取 `docs/V2实施清单.md`；长期算法方向读取 `docs/评测与项目理解.md`。
> 注意：本文件不再维护最新状态。文中“当前”“后续”等措辞如果与主文档冲突，按主文档为准。

## 0. 文档用途

这份文档回答一个问题：**真实 LLM 下，TRACE 的策略评测是否可复现、可审计、可对比。**

V1 证明的是单机闭环：MockLLM、三策略、trace、report、脚本评测。V2 要把它升级为评测系统：

```text
真实 LLM run -> 冻结 prompt/tool/runtime -> clean 代码生成最终测试集
-> bug 变体只 replay -> 指标落库 -> 前端展示实验结果 -> 可从 DB/artifact 重算审计
```

本文在精简前曾作为 V2 状态源；现在只保留历史上下文、旧失败流水和验收依据。仍然有约束力的规则应回看 `docs/V2实施清单.md` 的核心口径和代码/schema。

## 1. 当前状态

### 1.1 V2.1 状态

状态：**V2.1 基本收口，真实 LLM seeded bug experiment 已重跑并产出可审计证据；三策略均可评价、6/6 捕获、clean false positive 0.00。**

已完成并有证据：

- [x] 版本表与 runtime profile：`runtime_profiles`、`strategies`、`prompt_versions`、`tool_schema_versions`。
- [x] 普通 API run 支持真实 LLM provider/model 配置，resolved spec 冻结到 `strategy_snapshot`。
- [x] Agent 上下文从 AST 摘要升级为 AST 摘要 + 目标函数/路由源码片段。
- [x] Context Builder 已补 pytest fixture / existing tests 支持证据：`conftest.py` fixture 可被 AST 识别，source context 会携带 fixture 和已有测试样例片段，且不会掩盖目标源码缺失。
- [x] Context Builder 已补最小 Pydantic model 证据：`AnalyzeProjectOutput.models` 带字段名/类型/默认值/required，source context 会携带 model class 片段，供请求/响应字段生成使用。
- [x] Contract Guard 已补最小字段来源检查：当存在 Pydantic model 字段证据时，生成测试里的 `json={...}` 请求体字段必须来自模型字段，否则 `PIPELINE_REJECT`。
- [x] Contract Guard 已补最小 fixture 约束：生成测试函数参数必须来自项目 fixture 证据或 pytest 内置 fixture；`parametrize` 参数不误判为 fixture。
- [x] Contract Guard 已补最小业务 oracle 检查：路由测试只有 `2xx status_code` 断言时拒绝，要求至少有响应体/业务字段等额外断言证据。
- [x] Reflection prompt 已补失败上下文：失败 nodeid、异常类型、message、file:line 和 traceback 摘要会进入反思输入，减少靠猜修复。
- [x] Contract Guard 已补最小请求模型缺失阻断：路由测试使用 `json=...` 请求体但没有 Pydantic model 字段证据时 `PIPELINE_REJECT`；无请求体路由不误伤。
- [x] Plan-and-Execute 已补计划目标归一化：函数目标统一为函数名，路由目标统一为 `METHOD path`，避免 `get_price`、`/price/{item}`、`GET /price/{item}` 这种同一目标多名导致后续 Contract Guard 误判。
- [x] 单次 run report 展示测试清单、目标映射、断言摘要、失败分类、Reflection 证据、上下文完整性。
- [x] eval/experiment 表、demo dataset seed、inline canonical patch metadata 已落库。
- [x] Experiment runner 已实现 clean run、final test set artifact、clean replay、bug variant replay。
- [x] Replay 不调用 LLM；clean false positive 与 variant captured bug 分层落库。
- [x] Metrics 从 DB 聚合，返回 `rows` + `capture_matrix`，并与 V1 harness 口径对齐。
- [x] 前端已完成 Experiment List / Detail、capture matrix、clean run drilldown、replay detail、Dataset Detail、Comparison 真实 metrics 优先。
- [x] 从 `test_runs`、`pytest_case_results`、`test_replays`、artifact 对 capture 指标做了重算审计。
- [x] Generated test set artifact replay 前校验 hash；篡改 artifact 会让 experiment 失败。
- [x] Experiment `target_scope` 符号解析已修复：符号型目标会触发项目扫描，再映射到真实源码片段。
- [x] 缺目标源码证据时 clean run 标记 `context_incomplete_blocking` / `invalid_test_set`，不调用 LLM、不进入 mutation score。
- [x] 指标和前端已区分 `invalid_test_set`、`evaluable_zero_capture`、`no_bug_captured`。
- [x] V1/V2 后端回归通过：`conda run -n trace pytest backend/tests -q`，180 passed。
- [x] Contract Guard 参数化 / 类方法 case 名称归并：`test_x[...]` 与真实函数 `test_x` 对齐，`TestClass.test_x` 与类方法 `test_x` 对齐，不把 pytest 参数化实例或类前缀误判为元数据漂移。
- [x] Generate prompt 已前置硬约束：裸比较、2xx-only route oracle、未知 fixture 参数、错误 `patch(..., new_value)` 参数都会在生成前被明确告知。
- [x] Contract Guard 已补源码行为证据检查：函数 oracle 若可由目标源码和参数化常量静态求值且与断言矛盾，生成阶段拒绝；覆盖负数、阈值、舍入和普通参数化表。
- [x] Contract Guard 已补目标绑定漂移检查：生成测试不得重定义被测目标函数；route 目标可匹配 `route:GET /path` 这类带类型前缀元数据。
- [x] Contract Guard 已补 path 参数空段检查：`/x/{param}` 路由不把 `/x/` 当成 `param=""` 的 handler 业务用例。
- [x] Contract Guard 已补窄口径 route 响应 oracle 检查：对可静态求值的 FastAPI GET path-param、query string、`_PRICES` monkeypatch、`response.json()` 断言，若与 handler 源码行为矛盾则拒绝。
- [x] 生成阶段已补一次结构化 Contract Guard retry：第一次契约拒绝时带 violations 和上一版测试重试一次；仍失败才 `PIPELINE_REJECT`。
- [x] eval CLI 已支持 `--max-output-tokens`，并把值作为 experiment `llm_override.max_output_tokens` typed field 冻结，避免真实兼容端点长输出被默认 8192 截断。
- [x] 真实 LLM seeded bug experiment 已按 `repeats=3` 重跑：DeepSeek `openai_chat_compat/deepseek-v4-flash`，最新 experiment id `exp-8d9f99fe-e7b4-4d2f-b807-7a2f1857a798`，证据文件 `eval/results/comparison_real.json` / `eval/results/comparison_real.md`。
- [x] Demo seeded bug probe 已接入 DB experiment 与 script harness：variant replay 前验证 `probe` 在 clean/buggy 两侧分别等于 `clean_value`/`buggy_value`，失败会让 experiment failed；成功结果写入 `replay_metrics.probe_check` / harness variant 记录。

V2.1 剩余项：当前真实 seeded bug smoke 已完成收口；后续继续按 `docs/评测与项目理解.md` 做更深业务 oracle、多源 retrieval trace、flaky/repeat 等加固，不把这些未完成项伪装成 V2.1 已完成。

历史阻塞与当前待办：

- 2026-06-16 已尝试 `provider=openai/model=gpt-5`，本机 `OPENAI_API_KEY` 返回 401，错误信息已脱敏。
- 这次失败暴露的 runner 状态机问题已修复：clean run provider/LLM 失败时 experiment 必须 failed，不能伪装为 completed + 0 捕获。
- 2026-06-17 已接入 DeepSeek `provider=openai_chat_compat/model=deepseek-v4-flash`：
  - `/models` 探针通过，返回 `deepseek-v4-flash`、`deepseek-v4-pro`。
  - harness smoke 跑通，Direct v1 completed。
  - 当时的 V2 DB experiment 跑完，但不作为有效策略评测证据：`analyze_project` 未解析到 `shop/pricing.py` 目标源码，`source_context` 为 `context_incomplete`，生成测试在 clean replay 出现 import/collection error，最终 capture rate 为 0。这个 0 表示测试集不可评价，不表示模型真实捕获能力为 0。
- 2026-06-17 已修复上述有效性问题的工程口径：
  - `analyze_project` 对符号型 `target_scope` 不再当路径硬解析；符号目标会扫描项目，再由 source context bundle 切出目标源码。
  - Experiment runner 在 clean run 前做 target source context gate；缺目标源码时写 `context_incomplete_blocking` / `invalid_test_set`，跳过 LLM 生成和 variant replay。
  - clean replay collection/import error 写 `invalid_test_set`，不再压成捕获率 0。
  - Metrics 增加 `metric_status` 和 `invalid_test_set_count`；前端表格显式展示“不可评价 / 0 捕获 / 可评价”。
- 2026-06-17 已用 DeepSeek 重新跑真实 LLM seeded bug experiment：
  - experiment id：`exp-7b93cad4-2d84-4b5c-a035-d46800a39758`。
  - 证据文件：`eval/results/comparison_real.json`、`eval/results/comparison_real.md`。
  - Direct v1：`metric_status=ok`，6/6 捕获，false positive 0.00。
  - ReAct+Reflection v1：`metric_status=ok`，6/6 捕获，false positive 0.00。
  - Plan-and-Execute v1：`metric_status=invalid_test_set`，本轮不能解释为模型能力 0，必须继续修生成契约/上下文根因。
  - 本轮真实运行还暴露并修复了两个工程问题：路由目标 `GET /price/{item}` 与生成 case `/price/{item}` 的匹配归一化；真实 DB 中 trace step `name/error` 过长导致落库失败，现在短字段截断，完整细节保留在 payload。
- 2026-06-17 继续加固 Plan-and-Execute：
  - 当前连接的本地 DB 未保留 `exp-7b93cad4-2d84-4b5c-a035-d46800a39758` 的 clean rows，无法直接回看该次 `PIPELINE_REJECT` payload。
  - 先修确定性的坏结构：Plan 输出的 `target_ref` 进入执行前做 canonicalize，函数统一为 `function.name`，路由统一为 `METHOD path`。
  - 这一步不是 prompt 微调，而是把目标身份从自由文本收敛成唯一键，减少真实 LLM 在 handler/path/method path 之间摇摆导致的无效测试集。
- 2026-06-17 参数化 case / prompt 继续加固：
  - Plan-only 真实验证 `exp-95d494b1-1013-4274-a272-6e90296e7be8` 暴露了 Contract Guard 过严：pytest 参数化函数 `test_apply_discount` 的 case 元数据写成 `test_apply_discount[...]`，被误判为元数据漂移。
  - 已修 Contract Guard：case/test 函数对齐时把 `test_x[...]` 归并到 `test_x`；参数化测试不再被误杀。
  - 已把 Generate prompt 的硬约束前置：裸比较必须写成 `assert`、成功路由不能只有 2xx、不得添加未知 fixture 参数、`patch(..., new_value)` 不声明 mock 参数。
  - Plan-only 真实复验 `exp-718af122-24ce-43d0-bb66-78486df19393`：Plan-and-Execute `metric_status=ok`，6/6 捕获，false positive 0.00。
  - 上一轮三策略真实复验 `exp-ae74f9a4-66e7-4b33-aa81-2761c8c68457`：Direct / Plan / ReAct 均 `metric_status=ok` 且 6/6 捕获；Plan false positive 1.00，clean replay 失败点为 `tests/generated/test_item0.py::test_apply_discount_negative_total`，用于定位边界假设/源码行为证据问题。
- 2026-06-17 本轮继续按 failure nodeid 加固：
  - Plan false positive 根因不是 provider 或 replay，而是坏 oracle：`apply_discount(-50, True)` 被模型断言成按会员折扣返回 `-45.0`，但源码条件 `total >= 100` 不成立，clean 行为应为 `-50.0`。
  - 后续真实运行又暴露两个确定性坏结构：测试文件重定义 `loyalty_points` 导致没有测试项目源码；`route:GET /price/{item}` 被旧 route normalizer 误判为目标漂移。
  - Direct 运行暴露 route path 参数空段坏 oracle：`client.get("/price/")` 不能代表 `item=""` 调用 handler。
  - 已补 Contract Guard：源码行为窄口径静态求值、目标重定义拒绝、`route:` 前缀归一化、path 参数空段拒绝。
  - 已补生成阶段一次结构化 Contract Guard retry，带 violations 修正可修复契约错误。
  - 该轮三策略真实复验 `exp-aca4be4a-6580-4e83-9709-8ec561564724`：Direct / Plan / ReAct 均 `metric_status=ok`，均 6/6 捕获，clean false positive 均 0.00。
- 2026-06-17 多轮真实 `repeats=3` 复验继续暴露随机性问题，并已小步加固：
  - `exp-6226921b-20f2-4a4a-af12-33b2d8a33b30` 暴露 Direct route false positive：`_PRICES={"apple":5,"banana":30}` 时模型仍按会员折扣断言 `4.5/27.0`，但源码阈值 `total >= 100` 不成立；Plan 还出现未知 `client` fixture。
  - `exp-b59b1430-5d1c-4bec-a144-5da2e87964c2` / `exp-110d73ef-10bf-4c24-94e0-63dc5a2cb745` 暴露 class method case 元数据 `TestClass.test_x` 误判、route query string 静态求值误判、target drift retry 提示不够具体。
  - 一次真实 run 因兼容端点 `finish_reason=length` 在 8192 token 下失败；runner 正确 failed，没有伪装成 0 捕获。已补 eval CLI `--max-output-tokens` typed override。
  - `exp-dba0758a-facb-4c04-979f-ad39d258b5b6` 暴露普通参数化函数 oracle 漏拦：`apply_discount(999.99, True)` 被断言为未 round 的 `899.991`。
  - 已修：route 响应 oracle 静态校验、query string 解析与 stale response 清理、类方法 case 归并、参数化函数 oracle 静态校验、fixture/target/route-oracle retry 指令、`--max-output-tokens` 透传。
  - 最新三策略真实复验 `exp-8d9f99fe-e7b4-4d2f-b807-7a2f1857a798`：`repeats=3`，Direct / Plan / ReAct 均 `metric_status=ok`；三策略均 6/6 捕获；三策略 `clean_false_positive_rate=0.00`；三策略 `invalid_test_set_count=0`。
- 2026-06-18 按评测尺子方向补 probe 自检：
  - 抽出 `eval/harness/probes.py`，DB experiment 和 script harness 共用同一套 probe runner。
  - 函数 probe 只接受 AST 限定的“函数名 + 常量参数”调用，不用自由 `eval`；route probe 通过 FastAPI `TestClient` 取状态码。
  - variant replay 前验证 clean/buggy 两侧行为与 ground truth 一致；`clean_value == buggy_value` 或实际 probe 不匹配会让 experiment failed，避免等价/失效 mutant 混入分母。
  - replay metrics 写入 `probe_check`，用于审计该 captured bug 的 ground truth 前置条件。

最近验证：

| 命令                                                                                                                                                                                                                                                                                   | 结果                                                                                                |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `conda run -n trace pytest backend/tests/test_source_context.py backend/tests/test_v2_phase0_contracts.py -q`                                                                                                                                                                        | 21 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_stage4_experiment_runner.py -q`                                                                                                                                                                                                        | 13 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_source_context.py backend/tests/test_contract_guard.py backend/tests/test_stage4_experiment_runner.py -q`                                                                                                                              | 40 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_source_context.py -q`                                                                                                                                                                                                                  | 12 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_contract_guard.py -q`                                                                                                                                                                                                                  | 36 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_source_context.py backend/tests/test_contract_guard.py backend/tests/test_stage4_experiment_runner.py -q`                                                                                                                              | 51 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_reflection_prompt_context.py -q`                                                                                                                                                                                                       | 1 passed                                                                                            |
| `conda run -n trace pytest backend/tests/test_reflection_prompt_context.py backend/tests/test_source_context.py backend/tests/test_contract_guard.py backend/tests/test_stage4_experiment_runner.py -q`                                                                              | 54 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_contract_guard.py backend/tests/test_sqlalchemy_recorder.py -q`                                                                                                                                                                        | 31 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_eval_harness.py backend/tests/test_contract_guard.py backend/tests/test_sqlalchemy_recorder.py -q`                                                                                                                                     | 35 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_reflection_prompt_context.py backend/tests/test_source_context.py backend/tests/test_contract_guard.py backend/tests/test_sqlalchemy_recorder.py backend/tests/test_eval_harness.py backend/tests/test_stage4_experiment_runner.py -q` | 62 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_plan_execute_targets.py backend/tests/test_contract_guard.py backend/tests/test_e2e_mockllm.py::test_plan_execute_two_items -q`                                                                                                        | 32 passed                                                                                           |
| `conda run -n trace pytest backend/tests/test_stage4_experiment_runner.py backend/tests/test_plan_execute_targets.py -q`                                                                                                                                                             | 17 passed，2 warnings；沙箱内 conda env 不可写，外部执行取得结果                                    |
| `conda run -n trace pytest backend/tests/test_contract_guard.py backend/tests/test_plan_execute_targets.py backend/tests/test_e2e_mockllm.py::test_direct_pass -q`                                                                                                                    | 33 passed                                                                                           |
| `conda run -n trace python eval/harness/run_eval.py --real --provider openai_chat_compat --base-url $cfg.base_url --model $cfg.model --repeats 1`                                                                                                                                    | 上一轮 `exp-ae74f9a4-66e7-4b33-aa81-2761c8c68457`；三策略均可评价且 6/6，Plan false positive 1.00    |
| `conda run -n trace pytest backend/tests/test_e2e_mockllm.py -q`                                                                                                                                                                                                                    | 10 passed                                                                                           |
| `conda run -n trace python eval/harness/run_eval.py --real --provider openai_chat_compat --base-url $cfg.base_url --model $cfg.model --repeats 1`                                                                                                                                    | `exp-aca4be4a-6580-4e83-9709-8ec561564724`；三策略均可评价且 6/6，三策略 false positive 均 0.00     |
| `conda run -n trace pytest backend/tests/test_contract_guard.py backend/tests/test_e2e_mockllm.py backend/tests/test_eval_harness.py -q`                                                                                                                                              | 56 passed；沙箱内 conda env 不可写，外部执行取得结果                                                |
| `conda run -n trace pytest backend/tests/test_plan_execute_targets.py backend/tests/test_stage4_experiment_runner.py -q`                                                                                                                                                             | 17 passed，2 warnings；沙箱内 conda env 不可写，外部执行取得结果                                    |
| `conda run -n trace python eval/harness/run_eval.py --real --provider openai_chat_compat --base-url $cfg.base_url --model $cfg.model --max-output-tokens 16384 --repeats 3`                                                                                                           | `exp-8d9f99fe-e7b4-4d2f-b807-7a2f1857a798`；三策略均可评价，三策略均 6/6，三策略 false positive 0.00，三策略 invalid_test_set_count 0 |
| `conda run -n trace pytest backend/tests -q`                                                                                                                                                                                                                                         | 201 passed，2 warnings；沙箱内 conda env 不可写，外部执行取得结果                                   |
| `conda run -n trace pytest backend/tests/test_eval_harness.py -q`                                                                                                                                                                                                                    | 6 passed，1 warning；probe helper 与 script harness full_eval 通过                                  |
| `conda run -n trace pytest backend/tests/test_stage4_experiment_runner.py -q`                                                                                                                                                                                                        | 15 passed，2 warnings；probe gate 已进入 DB experiment replay 主路径                                |
| `conda run -n trace pytest backend/tests/test_stage3_evaluation_seed_and_api.py -q`                                                                                                                                                                                                  | 3 passed，1 warning                                                                                 |
| `conda run -n trace pytest backend/tests -q`                                                                                                                                                                                                                                         | 180 passed                                                                                          |
| `conda run -n trace pytest backend/tests -q`                                                                                                                                                                                                                                         | 本轮更新后尝试重跑，沙箱内 conda env 不可写；外部执行一次 180s 超时、一次被中止，未拿到新增全量结果 |
| `npm run typecheck`                                                                                                                                                                                                                                                                  | passed                                                                                              |
| `npm run build`                                                                                                                                                                                                                                                                      | passed                                                                                              |

### 1.2 V2.2 / 后续

以下为归档时的后续项快照，不再作为当前待办源；当前待办以 `docs/V2实施清单.md` 为准。

- [x] 项目理解 / 评测有效性最小加固：修 target scope 符号解析、目标源码 context gate、invalid metrics/frontend 状态。
- [x] 评测有效性 / probe：demo hand-crafted seeded bug 在 materialize/replay 前做 clean/buggy 行为自检，并把 `probe_check` 写入审计证据。
- [ ] 项目理解 / 评测有效性继续加固：按 `docs/评测与项目理解.md` 的阶段 B/C 方向，补 rg/ast-grep/LSP、多源 retrieval trace、更深的业务 oracle 证据校验。
- [ ] 评测有效性 / flaky 与 repeat：final test set 冻结前加入 flaky check，capture matrix 从 bool 升级到 repeat 命中比例。
- [ ] Docker executor：抽 executor type、加 Docker executor、runtime profile 支持 executor 配置、demo dataset 在 Docker 下跑通。
- [ ] Prompt version 页面。
- [ ] Tool schema version 页面。
- [ ] Runtime profile 管理页面。
- [ ] Dataset 创建表单。
- [ ] 多 demo dataset 和 seeded bug 扩展。
- [x] structured retry：生成阶段 Contract Guard violations 可进行一次结构化重试；仍失败才 `PIPELINE_REJECT`。
- [ ] final attempt / report / replay 的 records 协议进一步统一，减少 InMemoryRecorder 与 DB recorder 形状差异。

## 2. V2 边界

### 2.1 V2.1 必做

V2.1 是“真实 LLM + 评测落库”。它必须证明：

- 后端 API/worker 支持真实 LLM provider。
- Prompt、Tool Schema、Runtime Profile 正式版本化。
- 评测数据集、任务、seeded bug、bug variant、experiment、clean run、replay run 落库。
- `eval/harness/run_eval.py` 只是 CLI 包装或兼容入口；核心 clean/replay/metrics 走 service/API/DB。
- 后端提供 experiment API 和 DB 聚合 metrics。
- 前端真实 API 模式展示 experiment metrics，mock 只作为显式 fallback。
- 单次 run report 能说明测试质量，不只是运行统计。
- Agent 生成上下文能按目标读取必要源码片段，并在缺失时写风险。

### 2.2 V2.2 可做

V2.2 是加固项，不阻塞 V2.1：

- 本地 Docker executor。
- 每次 run 独立容器与独立工作目录。
- 默认禁网、限制 CPU/内存/进程数、只挂载必要目录。
- 仍然是本机执行，不做远程 Runner 注册、心跳、long-poll。

### 2.3 明确不做

这些留给 V3 或更后面：

- 远程/自托管 Runner API：`register`、heartbeat、next-job long-poll、runner job lease、presigned artifact upload。
- 多租户、权限系统、组织空间。
- GitHub App、自动拉 PR、完整 CI 集成。
- OpenTelemetry 全链路追踪。
- WebSocket 实时 trace 推送。
- Prompt 在线编辑器大后台。
- 覆盖率作为核心指标。

V2 可以为这些功能留字段和扩展点，但不能提前实现半套。

## 3. V2.1 收口清单

### 3.1 功能链路

- [x] 创建 runtime profile。
- [x] 创建 prompt/tool schema version。
- [x] 创建 strategy version 并绑定 prompt/tool schema。
- [x] API/worker 普通 run 跑通真实 LLM provider 路径。
- [x] 创建 experiment。
- [x] 选择 dataset、eval task、strategy versions、repeat、LLM provider 覆盖配置。
- [x] clean snapshot 上运行 Agent，生成最终测试集。
- [x] Reflection 只在 clean snapshot 上执行。
- [x] 冻结 generated test set artifact。
- [x] replay 同一批冻结测试到 clean snapshot。
- [x] replay 同一批冻结测试到 bug variant。
- [x] clean false positive、seeded bug capture、cost、token、tool call、耗时写入 DB。
- [x] `GET /api/v1/experiments/{experiment_id}/metrics` 返回 `rows` + `capture_matrix`。
- [x] 前端真实 API 模式展示 experiment metrics，并标明数据来源、provider、model、repeat count、mock/real。
- [x] 单次 run report 展示测试清单、目标映射、断言摘要、失败分类、Reflection 证据、上下文完整性。
- [x] 指标可从 DB/artifact 重算审计。
- [x] 真实 LLM provider/key/endpoint smoke：DeepSeek `openai_chat_compat` + `deepseek-v4-flash` 已验证可调用。
- [x] 修复 experiment `target_scope` 到源码证据的解析：seeded bug target 可映射到真实文件/符号，例如 `shop/pricing.py` 和目标函数；解析不到时写结构化失败，不能让模型盲写。
- [x] 加目标源码级 `context_incomplete_blocking`：缺目标源码时 clean run 标为不可评价，不调用 LLM，不进入 mutation score。
- [x] 指标状态区分 `invalid_test_set`、`no_bug_captured`、`evaluable_zero_capture`：clean collection/import error 不能被压扁成“捕获率 0”。
- [x] 前端 Experiment Detail / Comparison 显式显示 metrics 状态：可评价、不可评价、0 捕获。
- [x] 生成阶段最小 `PipelineReject`：拒绝 `assert True`、`assert x is not None`、skip/xfail、宽泛 except、空测试、cases 元数据漂移、目标绑定缺失/不匹配。
- [x] Context Builder 最小 fixture / existing tests 证据：扫描 `conftest.py` / test 文件中的 pytest fixture 和已有测试函数，按预算加入 source context，供生成阶段使用。
- [x] Context Builder 最小请求/响应模型证据：扫描 Pydantic model 字段并加入 source context，供生成阶段使用。
- [x] Contract Guard 最小字段来源检查：`json={...}` 请求字段必须来自已知 Pydantic model 字段。
- [x] Contract Guard 最小 fixture 约束：测试函数参数必须来自项目 fixture 证据或 pytest 内置 fixture。
- [x] Contract Guard 最小业务 oracle 检查：成功路由测试不能只有 `2xx status_code` 断言。
- [x] Reflection failure context：反思输入包含失败位置、异常、message 和 traceback 摘要，避免只靠 pytest 简短摘要修复。
- [x] Contract Guard 最小请求模型缺失阻断：路由测试使用 `json=...` 请求体但没有模型字段证据时拒绝。
- [x] Plan target identity 归一化：Plan-and-Execute 执行前把函数/路由 `target_ref` 收敛为 canonical ref，降低真实 LLM 目标别名造成的无效测试集。
- [x] Contract Guard 参数化 case 名称归并：`test_x[...]` 与真实函数 `test_x` 对齐。
- [x] Generate prompt 前置硬约束：裸比较、2xx-only route oracle、未知 fixture 参数、错误 `patch(..., new_value)` 参数都会在生成前被明确告知。
- [x] Contract Guard 源码行为窄口径检查：边界/负数/阈值/舍入类函数 oracle 能由目标源码静态求值时，矛盾断言拒绝。
- [x] Contract Guard 目标绑定漂移继续加固：拒绝重定义被测目标函数；`route:GET /path` 与 `GET /path` 归一；拒绝 path 参数空段假业务用例。
- [x] 生成阶段 structured retry：Contract Guard 第一次拒绝后带 violations 重试一次，仍失败才 `PIPELINE_REJECT`。
- [x] 真实 LLM seeded bug experiment smoke 重跑并记录 evidence：`exp-8d9f99fe-e7b4-4d2f-b807-7a2f1857a798`，`provider=openai_chat_compat/model=deepseek-v4-flash`，`repeats=3`，三策略均可评价且 6/6；三策略 clean false positive 均 0.00；三策略 invalid_test_set_count 均 0。
- [ ] 前端打开 Experiment Detail / Comparison，保存截图或等价 evidence。

### 3.2 工程验收

- [x] Alembic upgrade head 在干净库可用。
- [x] ORM metadata 与 Alembic schema 对齐。
- [x] V1 主测试仍通过。
- [x] V1 API 兼容。
- [x] 评测指标有单元测试和端到端测试。
- [x] provider 错误脱敏。
- [x] replay 不调用 LLM 有测试保护。
- [x] artifact hash 可校验。
- [x] replay 前读取 artifact 时校验 hash。
- [x] `eval/harness/run_eval.py` 与 experiment service 共享核心指标口径。
- [x] report schema 和前端展示有测试覆盖。
- [x] 源码片段读取有路径越界、大小限制和脱敏测试。

### 3.3 口径验收

- [x] false positive 只属于 clean run。
- [x] captured bug 只属于 variant replay。
- [x] replay 不重新生成测试。
- [x] replay 不调用 LLM。
- [x] 捕获率只认 clean 通过、variant assertion failure 的同一 nodeid。
- [x] ground truth 相关性在 V2 默认口径下依赖单点 canonical patch + 唯一命中校验。
- [x] 非单点变体必须补 matcher，不能套默认口径。
- [x] 0 捕获时 `cost_per_captured_bug = null`，状态写 `no_bug_captured`，不能写 0。
- [x] MockLLM 和 real LLM 指标分开标识。
- [x] `llm_override` 只作为覆盖层，最终 provider/model/temperature 只冻结在每个 clean run 的 `strategy_snapshot`。
- [x] 语义版 `reflection_valid_fix_rate` 没有人工或 reason 白名单时不展示；V2.1 只展示自动替代指标。
- [x] Mock/scripted DB 聚合结果与 V1 harness `metrics.py` 同口径一致。
- [x] 真实 LLM 不承诺固定数值，只承诺口径一致。
- [x] mock comparison 必须明确标记为 mock，不得当作真实 API 结果展示。
- [x] report 中的断言摘要和目标映射是质量证据，不等同于人工有效性评分。
- [x] 源码片段上下文只服务测试生成，不允许 Agent 修改业务源码。

## 4. 核心口径

### 4.1 Experiment 与 LLM 配置

Experiment 是评测执行单元。strategy 列表走 `experiment_strategy_versions` 多对多表，不塞数组字段当真相。

`llm_override` 是实验级覆盖层：

```text
strategy_versions 默认模型参数
-> experiments.llm_override 覆盖层
-> repeat 派生参数
-> resolved spec 冻结进 test_runs.strategy_snapshot
```

规则：

- API 请求不传 API key。
- key 只从环境变量或 secret manager 读。
- `llm_override` 可以入库，但不能包含 secret。
- 最终 provider/model/temperature 只在每个 clean run 创建时解析一次。
- 实际调用 LLM 的参数和 `strategy_snapshot` 必须来自同一份 resolved spec。
- provider 为 `mock` 才走 MockLLM；真实 provider 走 provider factory。
- clean run 如果 provider/LLM 调用失败，experiment 必须 failed，不能伪装成 completed + 0 捕获。

### 4.2 Clean run

Clean run 负责生成和定型测试集。

流程：

1. 从 eval task 合成 `test_plan.target_scope`。
2. 创建 `test_run`，绑定 clean `project_snapshot_id`、runtime profile、strategy version。
3. 解析并冻结 runtime/strategy/LLM snapshot。
4. Agent 基于 AST 摘要 + 目标源码片段生成测试。
5. 如果允许 Reflection，只允许在 clean snapshot 上修一次。
6. 选择 final attempt。
7. 从 final attempt 生成最终测试集 artifact。
8. 在 clean snapshot 上 replay 冻结测试集，判定 clean false positive。

规则：

- clean replay assertion failure 属于 clean false positive，不属于 captured bug。
- clean replay collection/import error、缺目标源码导致的 `context_incomplete_blocking` 属于 `invalid_test_set`，不解释为模型捕获能力 0。
- clean run provider/LLM 失败时 experiment failed；测试集不可评价时 clean run 可保留为无效 evidence，但不进入 mutation score。
- 生成测试目录必须受 path policy 限制；Agent 不允许写业务源码。

### 4.3 Replay run

Replay run 只执行冻结测试集。

规则：

- replay 不规划、不生成、不调用 LLM。
- replay 可以跑 clean snapshot，也可以跑 bug variant snapshot。
- bug variant replay 前从 artifact 读取测试集，并校验 content hash。
- 如果 artifact hash mismatch，experiment failed。
- variant patch 必须从 clean snapshot 唯一应用；命中 0 次或多次都失败。
- replay 的 pytest 结果必须能映射到原始 nodeid。

### 4.4 captured bug

默认 V2.1 口径：

```text
clean replay passed nodeid
AND same nodeid has assertion failure on variant replay
=> captured_bug = true
```

只看 `experiment_replay_runs.captured_bug` 聚合捕获率。不要从前端二次解释 captured bug。

### 4.5 指标

V2.1 必算指标：

- `seeded_bug_capture_rate`
- `clean_false_positive_rate`
- `avg_tokens`
- `avg_tool_calls`
- `avg_duration_ms`
- `pytest_collection_success_rate`
- `cost_per_captured_bug`
- `reflection_contract_pass_rate`
- `reflection_acceptance_rate`

成本规则：

- 有捕获：`cost_per_captured_bug = total_tokens / total_captured`
- 0 捕获：`cost_per_captured_bug = null`，`cost_per_captured_bug_status = no_bug_captured`
- 指标有效性：`metric_status = ok | evaluable_zero_capture | invalid_test_set`。
- 不可评价测试集：如果 clean replay collection/import error、目标源码缺失导致 `context_incomplete_blocking`，或 final test set 没有稳定 clean-passed nodeid，则 capture rate 不应解释为模型捕获能力 0；必须写 `invalid_test_set` 或等价状态，并暴露原因。

### 4.6 Report quality

普通 run report 必须包含：

- 测试清单：生成了哪些测试，nodeid 是什么。
- 目标映射：测试对应哪些函数/路由/模块。
- 断言摘要：每个测试验证什么行为。
- 失败分类：assertion、fixture、import、collection、timeout 等。
- Reflection 证据：是否使用、是否通过契约、采纳了哪次 attempt。
- 上下文完整性：是否读取目标源码片段，缺失时写 `context_incomplete` 风险。

没有人工评分时，不展示 `meaningful_assertion_rate` 为已完成指标。

### 4.7 Source context

Agent 生成上下文规则：

- 先跑 `analyze_project` 得到 AST 摘要。
- 再按目标函数/路由读取源码片段。
- 读取范围受项目根、allowlist、denylist、文件大小、总预算、snippet 数量限制。
- 路径越界、找不到目标、预算截断时写 `context_incomplete` 或 partial 风险。
- seeded bug experiment 中，目标函数/路由必须能解析到真实 `source_path` 和源码片段；解析不到目标源码时应进入 `context_incomplete_blocking` / `PipelineReject`，不能继续生成用于 mutation score 的测试集。
- target scope 解析、动态代码情报、Context Builder、Contract Guard 的后续设计以 `docs/评测与项目理解.md` 为方向源。
- 不用无边界多轮探索弥补上下文质量。

## 5. 数据模型摘要

V2 在 V1 14 张表基础上扩展，不推倒重来。

V1 基础表：

```text
projects, project_snapshots, strategy_versions, test_plans, test_runs,
run_plan_items, run_attempts, generated_test_files, generated_test_cases,
pytest_case_results, trace_steps, run_events, run_artifacts, test_reports
```

V2 新表：

| 表                               | 用途                                  | 关键约束                                             |
| -------------------------------- | ------------------------------------- | ---------------------------------------------------- |
| `runtime_profiles`             | 可复用执行环境配置                    | run 创建时展开进 `test_runs.runtime_snapshot`      |
| `strategies`                   | 策略逻辑父表                          | Direct / Plan-and-Execute / ReAct 等父级语义         |
| `prompt_versions`              | 不可变 prompt 正文版本                | 内容变化创建新 version，记录 content hash            |
| `tool_schema_versions`         | 不可变工具 schema 版本                | schema 来自 Pydantic 或显式 JSON schema              |
| `eval_datasets`                | 评测数据集版本                        | dataset version 不可变                               |
| `eval_tasks`                   | 评测任务                              | `target_scope` 是捕获率分母来源                    |
| `seeded_bugs`                  | 缺陷语义定义                          | 不直接保存 patch 真相                                |
| `bug_variants`                 | 具体 bug 变体                         | V2 默认 `canonical_kind=patch`，patch 必须唯一命中 |
| `experiments`                  | 一次实验定义                          | 失败是系统失败，不是策略捕获低                       |
| `experiment_strategy_versions` | experiment 与 strategy version 多对多 | `(experiment_id, strategy_version_id)` 唯一        |
| `experiment_clean_runs`        | clean 生成与定型记录                  | false positive 只在 clean 层判断                     |
| `test_replays`                 | 冻结测试集 replay 记录                | `llm_calls=0`                                      |
| `experiment_replay_runs`       | bug variant 捕获结果                  | 捕获率只看这里                                       |

关键迁移规则：

- `project_snapshots` 补 `source_ref`、`commit_sha`、`dependency_summary`、`entrypoint_guess`、`analysis_summary`。
- `strategy_versions` 补 `strategy_id`、`version`、`prompt_version_id`、`tool_schema_version_id`、`temperature`、`is_locked`。
- `test_runs` 新 run 必须绑定 runtime profile 或冻结完整 runtime snapshot。
- `run_artifacts` metadata 必须包含 kind、created_by、experiment_id、clean_run_id/replay_id、content_role、relative_paths、hash_algorithm。

暂不建：

| 表                       | 原因                                                         |
| ------------------------ | ------------------------------------------------------------ |
| `runners`              | V3 远程 Runner 才需要                                        |
| `runner_jobs`          | V3 job lease 才需要                                          |
| `experiment_artifacts` | V2 先用 `run_artifacts` + metadata                         |
| `human_scores`         | 可先写回 `generated_test_cases.human_meaningfulness_score` |

## 6. API 摘要

Runtime profile：

```text
POST   /api/v1/projects/{project_id}/runtime-profiles
GET    /api/v1/projects/{project_id}/runtime-profiles
GET    /api/v1/runtime-profiles/{runtime_profile_id}
```

Prompt / Tool schema：

```text
POST   /api/v1/prompt-versions
GET    /api/v1/prompt-versions/{prompt_version_id}
POST   /api/v1/tool-schema-versions
GET    /api/v1/tool-schema-versions/{tool_schema_version_id}
```

Strategy：

```text
POST   /api/v1/strategies
POST   /api/v1/strategies/{strategy_id}/versions
GET    /api/v1/strategy-versions/{version_id}
GET    /api/v1/strategies
GET    /api/v1/strategies/{strategy_id}/versions
```

Dataset / Bug：

```text
POST   /api/v1/eval-datasets
GET    /api/v1/eval-datasets
GET    /api/v1/eval-datasets/{dataset_id}
POST   /api/v1/eval-datasets/{dataset_id}/tasks
GET    /api/v1/eval-datasets/{dataset_id}/tasks
POST   /api/v1/eval-tasks/{task_id}/seeded-bugs
GET    /api/v1/eval-tasks/{task_id}/seeded-bugs
POST   /api/v1/seeded-bugs/{seeded_bug_id}/variants
GET    /api/v1/seeded-bugs/{seeded_bug_id}/variants
```

Experiment：

```text
POST   /api/v1/experiments
GET    /api/v1/experiments
GET    /api/v1/experiments/{experiment_id}
POST   /api/v1/experiments/{experiment_id}/runs
POST   /api/v1/experiments/{experiment_id}/cancel
GET    /api/v1/experiments/{experiment_id}/clean-runs
GET    /api/v1/experiments/{experiment_id}/replay-runs
GET    /api/v1/experiments/{experiment_id}/metrics
```

规则：

- `POST /runs` 异步入队，不阻塞 HTTP。
- retry 创建新 experiment 或新 run batch，不覆盖历史结果。
- cancel V2 可以先软取消；执行中 subprocess/Docker 中断作为加固项。

## 7. Runtime、Artifact 与前端

### 7.1 Runtime

V2.1 继续使用 subprocess executor，但必须诚实：

- pytest 命令来自 runtime snapshot。
- timeout 可强制。
- CPU、内存、进程数限制在 V2.1 是声明性记录，不是强制隔离。
- 不允许把 V2.1 subprocess 说成安全沙箱。
- Docker executor 后置到 V2.2。

### 7.2 Artifact

Artifact 是复现和审计的一部分，不是日志垃圾桶。

规则：

- generated test set artifact 写入后计算 sha256。
- DB 保存 URI、content hash、size、metadata。
- replay 前读取 artifact 并校验 hash。
- hash 不匹配时 experiment failed。
- artifact metadata 至少包含：
  - `kind`
  - `created_by`
  - `experiment_id`
  - `clean_run_id`
  - `replay_id`
  - `content_role`
  - `relative_paths`
  - `hash_algorithm`

### 7.3 前端

V2 前端主屏从单 run 扩展到 experiment。

已完成：

- Experiment List
- Experiment Detail
- Capture Matrix
- Clean Run Drilldown
- Replay Detail
- Dataset Detail
- Comparison 真实 experiment metrics 优先，mock fallback 显式标记
- Run Console report quality 展示

后置：

- Prompt version 页面
- Tool schema version 页面
- Runtime profile 管理页面
- Dataset 创建表单

展示口径：

- 可以说 experiment metrics 来自数据库聚合。
- 可以说 replay 不调用 LLM。
- 可以说 mock/scripted 与 real LLM 分开展示。
- 不要说某策略永远优于另一策略。
- 不要把 demo 捕获率外推到任意项目。
- 不要把 subprocess/Docker 说成绝对安全沙箱。
- 不要把 mock comparison 当真实 API 评测结果。

## 8. 实施历史

历史只记录已完成阶段的证据，不再作为当前待办的第二份真相。

| 阶段                             | 状态   | 验收证据                                                                                                                                                                                                   |
| -------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 阶段 0：口径和 schema            | 已完成 | `backend/app/schemas/evaluation.py`、`eval/fixtures/mock_experiment_v2.json`、`backend/tests/test_v2_phase0_contracts.py`                                                                            |
| 阶段 1：版本表和 runtime profile | 已完成 | `backend/tests/test_stage1_versioning_runtime_profiles.py`、`backend/tests/test_v2_versioning_api.py`、`backend/tests/test_executor_runtime_command.py`、`backend/tests/test_schema_migrations.py` |
| 阶段 2：真实 LLM 接入普通 run    | 已完成 | `backend/tests/test_stage2_source_context_and_report_quality.py`、`backend/tests/test_source_context.py`、`backend/tests/test_llm_runtime_config.py`；普通真实 run smoke 曾产出 trace/report         |
| 阶段 3：评测表和 seed 数据       | 已完成 | `backend/tests/test_stage3_evaluation_seed_and_api.py`、`backend/tests/test_schema_migrations.py`                                                                                                      |
| 阶段 4：Experiment runner        | 已完成 | `backend/tests/test_stage4_experiment_runner.py`，含 DB metrics parity、artifact hash、DB/artifact 重算审计、provider failure 状态机                                                                     |
| 阶段 5：前端 Experiment UI       | 已完成 | `npm run typecheck`、`npm run build`，5186 端口 smoke                                                                                                                                                  |
| 阶段 6：Docker executor          | 未开始 | V2.2 后置                                                                                                                                                                                                  |

阶段 0 锁定的关键契约：

- `llm_override` 只是覆盖层，resolved spec 冻结进每个 clean run。
- captured bug 默认依赖单点 canonical patch + 唯一命中校验。
- 语义版 `reflection_valid_fix_rate` 没有人工/白名单时不展示。
- report 必须展示质量证据。
- source context 读取不能突破项目根和生成测试目录安全边界。
- metric status 必须区分 `invalid_test_set`、`evaluable_zero_capture`、`no_bug_captured`。
- 生成测试必须过系统层 Contract Guard；明显弱断言和目标绑定漂移必须 `PIPELINE_REJECT`，不能靠 prompt 自律。

阶段 4 对齐的指标细节：

- 捕获率按 repeats 取均值和总体标准差。
- 0 捕获时 `cost_per_captured_bug` 为 null / `no_bug_captured`。
- 无效测试集不计入 mutation score，并通过 `metric_status=invalid_test_set` 暴露。
- `PIPELINE_REJECT` 产生的 clean unit 记为 `invalid_test_set`，不 replay bug variants。
- replay 捕获仍按 pytest nodeid 对齐；生成阶段 cases 元数据允许 `test_x[...]` 归并到真实函数 `test_x`，避免把参数化实例误判成元数据漂移。
- Mock/scripted DB 聚合结果与 V1 harness `metrics.py` 同口径一致。

## 9. 风险与后续处理

### 9.1 已化解风险

- MockLLM 自动化测试保留。
- experiment 支持 repeats。
- 指标展示均值和标准差。
- 指标聚合有单元测试和端到端测试。
- replay 逻辑和报告逻辑不再各算一套核心口径。
- artifact 写入后计算 hash，DB 保存 hash 和 URI。
- replay 前校验 generated test set artifact hash。
- V2.1 不依赖 Docker。
- Docker executor 明确后置到 V2.2。
- Experiment Detail 是前端主屏。
- Prompt/runtime 管理页面后置。
- clean/replay/metrics 已落到 service/API/DB。
- report quality 已展示测试质量证据。
- source context 已按目标读取源码片段，并标记 `context_incomplete`。
- experiment `target_scope` 符号解析已修复，seeded bug 目标可映射到真实 source path / symbol / snippet。
- 缺目标源码时已进入 `context_incomplete_blocking` / `invalid_test_set`，不调用 LLM，不进入 mutation score。
- clean replay collection/import error 已标为 `invalid_test_set`，不再被展示成捕获率 0。

### 9.2 剩余风险

- 真实 LLM seeded bug experiment 已重跑出 DB experiment evidence；最新三策略均可评价、6/6 捕获、clean false positive 0.00，且 `repeats=3` 下 invalid_test_set_count 为 0。但这是 demo seeded bug smoke，不应外推成任意项目策略结论。
- 项目理解硬门禁目前覆盖目标源码缺失、clean replay collection/import error、最小弱断言/目标绑定、`json={...}` 字段来源、fixture 约束、成功路由 status-only oracle、`json=...` 请求体缺模型字段证据、可静态求值的函数/路由源码行为矛盾 oracle、目标重定义、route 前缀归一和 path 参数空段；更深的业务 oracle 和多源 retrieval trace 还未完成。
- structured retry 目前只做生成阶段 Contract Guard violations 的一次重试；还没有覆盖 broader repair loop 或 flaky check。Demo hand-crafted seeded bug 的 probe 二次校验已接入，但 auto mutant / selected mutant 的 matcher 分层还未做。
- final attempt helper、InMemoryRecorder、DB recorder 的 records 协议还可以进一步统一，减少未来口径漂移。
- Docker executor 未开始；V2.1 subprocess 不能声称是安全沙箱。
- 多 dataset / 自动 mutation / 更大规模 replay 尚未做。

## 10. 下一步

V2.1 seeded bug smoke 已收口。随后继续按 `docs/评测与项目理解.md` 推进：

1. 补多源 retrieval trace，并继续强化请求/响应模型与 fixture 证据。
2. 继续做更深业务 oracle 证据校验。
3. 接 flaky check / repeat 捕获比例，继续加固 mutation score。
4. 前端打开 Experiment Detail / Comparison，保存截图或等价 evidence。

完成后再进入 V2.2：

1. 抽 executor type。
2. 加 Docker executor。
3. runtime profile 支持 executor 配置。
4. demo dataset 在 Docker executor 下跑通。
5. 文档继续诚实：Docker 是隔离加固，不是绝对安全。

## 11. 结论

V2 的价值不在“多几张表”。

它的价值在于：别人问“你说某策略更好，证据在哪”时，TRACE 能从 experiment metrics 追到 strategy、repeat、clean run、replay、seeded bug、nodeid、artifact、trace 和 pytest case result。

别人问“这份测试质量如何”时，TRACE 能说清楚测试目标、断言、失败原因、Reflection 证据和上下文来源。

这才是 V2 要打穿的东西。其他都先靠边。
